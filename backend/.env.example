DATABASE_URL=postgres://user:password@localhost:5432/portfolio
PORT=8080
VITE_PORT=5173
# JWT signing secret (32+ chars)
SESSION_SECRET=changeme_replace_with_strong_secret
# Optional fallback if you prefer a different env name
JWT_SECRET=changeme_replace_with_strong_secret
