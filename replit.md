# Portfolio CMS

A full-stack portfolio CMS for a CS graduate/AI-assisted developer with a public-facing portfolio site and a protected admin dashboard.

## Run & Operate

- `pnpm --filter @workspace/backend run dev` — run the API server (port 8080)
- `pnpm --filter @workspace/frontend run dev` — run the frontend dev server (port 21113)
- `pnpm run typecheck` — full typecheck across all packages
- `pnpm --filter @workspace/backend/api-spec run codegen` — regenerate API hooks and Zod schemas from the OpenAPI spec
- `pnpm --filter @workspace/backend run push` — push DB schema changes (dev only)
- Required env: `DATABASE_URL` — Postgres connection string, `SESSION_SECRET` — session signing key

## Stack

- pnpm workspaces, Node.js 24, TypeScript 5.9
- Frontend: React 19 + Vite 6, Tailwind CSS, shadcn/ui, React Query, React Router
- API: Express 5 (port 8080)
- DB: PostgreSQL + Drizzle ORM
- Validation: Zod (`zod/v4`), `drizzle-zod`
- Auth: Local JWT auth with bcrypt and HTTP-only cookie
- API codegen: Orval (from OpenAPI spec)
- Build: esbuild (ESM bundle)

## Where things live

- `frontend/` — React+Vite SPA (`@workspace/frontend`, port 21113)
  - `frontend/src/pages/` — public portfolio pages + admin dashboard
  - `frontend/src/api/` — inlined API client (React Query hooks, generated from OpenAPI)
  - `frontend/src/lib/auth/` — auth hooks and current login flow for admin session state
  - `frontend/src/components/` — shared UI components (shadcn/ui)
- `backend/` — Express 5 API server (`@workspace/backend`, port 8080)
  - `backend/src/db/` — Drizzle ORM schema + migrations (was `lib/db`)
  - `backend/src/validators/` — Zod schemas generated by Orval (was `lib/api-zod`)
  - `backend/src/routes/` — API route handlers
  - `backend/src/middlewares/` — auth, session, logging middleware
  - `backend/api-spec/` — OpenAPI spec + Orval codegen config (was `lib/api-spec`)
- `scripts/` — utility scripts (post-merge, etc.)
- `docs/` — architecture docs, API documentation, deployment guide

## Architecture decisions

- **Flat package layout**: `frontend/` and `backend/` at repo root instead of `artifacts/*/` + `lib/*`. Simpler mental model; same pnpm workspace mechanics.
- **Inlined shared libs**: `lib/db`, `lib/api-zod`, `lib/api-client-react`, `lib/replit-auth-web`, `lib/api-spec` were merged into `backend/src/` and `frontend/src/` to eliminate cross-package build ordering. TypeScript path aliases (`@workspace/db` → `./src/db/index.ts`) keep import paths stable.
- **esbuild alias map**: backend `build.mjs` has explicit `alias` entries for `@workspace/db` and `@workspace/api-zod` because esbuild doesn't read TypeScript `paths`.
- **Vite hoist pattern**: `.npmrc` hoists `vite`, `@vitejs/*`, `@tailwindcss/*`, `@replit/*` to workspace root so Vite's `node_modules/.vite-temp` config runner can find them.
- **Path-based routing**: Replit's external proxy routes `/api` → backend (8080) and `/` → frontend (21113) via artifact.toml. Internal `localhost:80` is the backend directly.

## Product

- **Public portfolio**: Home, About, Projects, Skills, Experience, Education, Certifications, Blog, Contact pages
- **Admin dashboard**: Protected CMS for managing all portfolio content (JWT auth required)

## User preferences

- Flat folder layout: `frontend/` and `backend/` at repo root (not under `artifacts/`)

## Gotchas

- `localhost:80` inside the container routes directly to the backend (Express, port 8080). The Replit path proxy (`/` → 21113, `/api` → 8080) only applies to external access through the Replit domain.
- esbuild doesn't read `tsconfig.json` `paths` — always mirror them in `build.mjs` `alias`.
- After changing `.npmrc` hoist patterns, must `rm -rf node_modules && pnpm install` (pnpm won't auto-remove without a TTY).
- Do NOT add Vite proxy config to reach the backend — the shared Replit proxy handles it for external access.
- `useGetCurrentUser` returns `{ data: { user: {...} | null } }` — check `data?.user`, not `data`.

## Pointers

- See the `pnpm-workspace` skill for workspace structure, TypeScript setup, and package details
- DB schema source of truth: `backend/src/db/schema/`
- API contract source of truth: `backend/api-spec/openapi.yaml`
